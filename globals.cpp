#include "globals.h"
#include <sstream>
#include <iostream>

using namespace std;

stringstream stringstrm(stringstream::in | stringstream::out);
int done = 0;
int capture_dev = 0;
int restart_server = 0;
int width = 640;
int height = 480;
